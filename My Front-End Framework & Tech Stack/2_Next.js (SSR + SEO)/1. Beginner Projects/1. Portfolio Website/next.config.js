/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // For next/image external sources (if needed)
    // domains: ['example.com'],
  }
};

module.exports = nextConfig;
